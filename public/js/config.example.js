// Local alternative to `npm run config`: copy this file to config.js and fill in.
// Use the public anon (publishable) key only — never the service role key.
window.APP_CONFIG = {
  SUPABASE_URL: '',
  SUPABASE_ANON_KEY: '',
};
